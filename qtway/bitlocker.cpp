#include "bitlocker.h"

// Copyright 2013-2022 Pasquale J. Rinaldi, Jr.
// Distrubted under the terms of the GNU General Public License version 2

/*
void InitializeBitlockerVolume()
{
    char* userpassword = "bittest123";
    libbde_volume_t* bitvolume = NULL;
    libbde_error_t* biterror = NULL;
    int err = libbde_volume_initialize(&bitvolume, &biterror);
    err = libbde_volume_set_utf8_password(bitvolume, (uint8_t*)userpassword, 10, &biterror);
    err = libbde_volume_is_locked(bitvolume, &biterror);
}
*/
